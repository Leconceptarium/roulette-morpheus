<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 40px 20px;
        }

        .header {
            text-align: center;
            color: white;
            margin-bottom: 50px;
        }

        .header h1 {
            font-size: 42px;
            margin-bottom: 10px;
            text-shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
        }

        .header p {
            font-size: 18px;
            opacity: 0.9;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        .grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
            margin-bottom: 40px;
        }

        .card {
            background: white;
            border-radius: 15px;
            padding: 30px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            transition: transform 0.3s ease;
        }

        .card:hover {
            transform: translateY(-5px);
        }

        .card-icon {
            font-size: 48px;
            margin-bottom: 20px;
        }

        .card h2 {
            color: #333;
            font-size: 24px;
            margin-bottom: 15px;
        }

        .card p {
            color: #666;
            line-height: 1.6;
            margin-bottom: 20px;
        }

        .btn {
            display: inline-block;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 12px 30px;
            border-radius: 25px;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s ease;
            border: none;
            cursor: pointer;
        }

        .btn:hover {
            transform: scale(1.05);
            box-shadow: 0 5px 15px rgba(102, 126, 234, 0.4);
        }

        .btn-secondary {
            background: #f0f0f0;
            color: #333;
        }

        .qr-section {
            background: white;
            border-radius: 15px;
            padding: 40px;
            text-align: center;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            margin-bottom: 40px;
        }

        .qr-section h2 {
            color: #333;
            font-size: 28px;
            margin-bottom: 20px;
        }

        .qr-code {
            background: #f9f9f9;
            padding: 20px;
            border-radius: 10px;
            display: inline-block;
            margin: 20px 0;
        }

        .url-display {
            background: #f0f0f0;
            padding: 15px;
            border-radius: 8px;
            margin: 20px 0;
            font-family: monospace;
            font-size: 16px;
            word-break: break-all;
        }

        .stats-preview {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border-radius: 15px;
            padding: 40px;
            text-align: center;
            margin-bottom: 40px;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-top: 30px;
        }

        .stat-box {
            background: rgba(255, 255, 255, 0.2);
            backdrop-filter: blur(10px);
            padding: 20px;
            border-radius: 10px;
        }

        .stat-number {
            font-size: 36px;
            font-weight: bold;
            margin-bottom: 10px;
        }

        .stat-label {
            font-size: 14px;
            opacity: 0.9;
        }

        .footer {
            text-align: center;
            color: white;
            padding: 20px;
            font-size: 14px;
            opacity: 0.8;
        }

        @media (max-width: 768px) {
            .header h1 {
                font-size: 32px;
            }

            .grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>🎭 <%= businessName %></h1>
        <p>Système de Gestion des Avis Google</p>
    </div>

    <div class="container">
        <!-- Section QR Code -->
        <div class="qr-section">
            <h2>📱 QR Code pour les Avis</h2>
            <p>Scannez ce QR code pour accéder directement à la page d'avis Google</p>
            
            <div class="qr-code">
                <div id="qrcode"></div>
            </div>

            <div class="url-display" id="reviewUrl">
                Chargement de l'URL...
            </div>

            <button class="btn" onclick="copyUrl()">📋 Copier le lien</button>
            <a href="/avis" class="btn btn-secondary" style="margin-left: 10px;">🔗 Tester le lien</a>
        </div>

        <!-- Fonctionnalités -->
        <div class="grid">
            <div class="card">
                <div class="card-icon">📊</div>
                <h2>Statistiques</h2>
                <p>Suivez vos performances : nombre de clics, taux de conversion, avis collectés.</p>
                <a href="/admin/stats?password=morpheus2024" class="btn">Voir les stats</a>
            </div>

            <div class="card">
                <div class="card-icon">💬</div>
                <h2>Envoyer des SMS</h2>
                <p>Envoyez automatiquement des demandes d'avis par SMS à vos clients.</p>
                <a href="/admin/send-sms?password=morpheus2024" class="btn">Envoyer SMS</a>
            </div>

            <div class="card">
                <div class="card-icon">⚙️</div>
                <h2>Documentation</h2>
                <p>Consultez le guide d'utilisation et les instructions d'installation.</p>
                <a href="https://github.com/votre-repo/morpheus-reviews" class="btn" target="_blank">Voir le README</a>
            </div>
        </div>

        <!-- Aperçu statistiques -->
        <div class="stats-preview">
            <h2>📈 Aperçu des Performances</h2>
            <div class="stats-grid">
                <div class="stat-box">
                    <div class="stat-number" id="totalClicks">-</div>
                    <div class="stat-label">Clics totaux</div>
                </div>
                <div class="stat-box">
                    <div class="stat-number" id="totalSMS">-</div>
                    <div class="stat-label">SMS envoyés</div>
                </div>
                <div class="stat-box">
                    <div class="stat-number" id="conversionRate">-%</div>
                    <div class="stat-label">Taux de conversion</div>
                </div>
                <div class="stat-box">
                    <div class="stat-number" id="clicksWeek">-</div>
                    <div class="stat-label">Clics (7 jours)</div>
                </div>
            </div>
        </div>
    </div>

    <div class="footer">
        🔮 Morpheus Experience © 2024 - Escape Game • Boutique Mystique • Salon de Thé<br>
        Strasbourg, France
    </div>

    <!-- QR Code library -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/qrcodejs/1.0.0/qrcode.min.js"></script>
    
    <script>
        // Récupérer l'URL d'avis
        fetch('/api/review-url')
            .then(res => res.json())
            .then(data => {
                const url = data.shortUrl;
                document.getElementById('reviewUrl').textContent = url;

                // Générer le QR code
                new QRCode(document.getElementById('qrcode'), {
                    text: url,
                    width: 256,
                    height: 256,
                    colorDark: '#333333',
                    colorLight: '#ffffff',
                    correctLevel: QRCode.CorrectLevel.H
                });
            })
            .catch(err => {
                console.error('Erreur:', err);
                document.getElementById('reviewUrl').textContent = 'Erreur lors du chargement de l\'URL';
            });

        // Copier l'URL dans le presse-papier
        function copyUrl() {
            const url = document.getElementById('reviewUrl').textContent;
            navigator.clipboard.writeText(url).then(() => {
                alert('✓ Lien copié dans le presse-papier !');
            }).catch(err => {
                console.error('Erreur lors de la copie:', err);
            });
        }

        // Charger les statistiques
        // Note: Nécessite l'authentification admin, donc on laisse vide par défaut
        // Décommenter si vous voulez afficher les stats publiquement
        /*
        fetch('/admin/api/stats?password=morpheus2024')
            .then(res => res.json())
            .then(data => {
                if (data.success) {
                    document.getElementById('totalClicks').textContent = data.data.global.totalClicks;
                    document.getElementById('totalSMS').textContent = data.data.global.totalSMS;
                    document.getElementById('conversionRate').textContent = data.data.global.conversionRate + '%';
                    document.getElementById('clicksWeek').textContent = data.data.global.clicksLast7Days;
                }
            })
            .catch(err => console.error('Erreur stats:', err));
        */
    </script>
</body>
</html>
